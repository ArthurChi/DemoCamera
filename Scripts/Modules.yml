include:
  - Templates.yml
targets:
  Authority:
    templates:
      - module_framework
    templateAttributes:
      COMPONENT_NAME: "Authority"

  Galilei:
    templates:
      - module_framework
    templateAttributes:
      COMPONENT_NAME: "Galilei"
    dependencies:
      - target: "Authority"
  
  MediaBox:
    templates:
      - module_framework
    templateAttributes:
      COMPONENT_NAME: "MediaBox"
    dependencies:
      - target: "Vincent"

  Vincent:
    templates:
      - module_framework
    templateAttributes:
      COMPONENT_NAME: "Vincent"
    # dependencies:
      # - target: ""   